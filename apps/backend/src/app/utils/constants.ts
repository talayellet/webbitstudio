/**
 * Default user ID when no ID or email is available
 */
export const DEFAULT_USER_ID = 'unknown' as const;

/**
 * Session max age in seconds (30 days)
 */
export const SESSION_MAX_AGE = 30 * 24 * 60 * 60;
