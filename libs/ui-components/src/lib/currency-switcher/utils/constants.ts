/**
 * Default fallback strings for Currency Switcher component
 * These are used when no localized ariaLabel prop is provided
 */
export const CURRENCY_SWITCHER_DEFAULTS = {
  ARIA_LABEL: 'Select currency. Current:',
} as const;
